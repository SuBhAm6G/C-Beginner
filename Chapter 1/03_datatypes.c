#include<stdio.h>

int main(){
    // int a;
    // a = 1;
    int a = 2; //both is valid

    float b = 9.34;

    char c;
    c = 'u';//char must be in a single comma
    
    printf ("%d\n", a);
    printf ("%f\n", b);
    printf ("%c\n", c);
    return 0;
}