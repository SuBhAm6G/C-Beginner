#include<stdio.h>

int main(){
int length, width;

printf("enter length =");
scanf("%d",&length);

printf("enter width =");
scanf("%d",&width);

printf("area=%d", length*width);
    return 0;
}