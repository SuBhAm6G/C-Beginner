#include<stdio.h>

int main(){
    int a; //variable declaration
    scanf("%d", &a);
    printf("Yo boy %d", a); //%d is used to represent the signed decimal variable, its like label telling
    return 0;
}