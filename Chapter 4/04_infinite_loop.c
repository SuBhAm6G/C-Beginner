#include<stdio.h>

int main(){
     int i = 0; 
     while (1<10) { //this statement remains always true
        printf("the value of i is %d\n", i); 
        i++;//incremental operator
}
    return 0;
}